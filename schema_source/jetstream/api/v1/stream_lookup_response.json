{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/jetstream/api/v1/stream_lookup_response.json",
  "description": "A response from the JetStream $JS.API.STREAM.LOOKUP API",
  "title": "io.nats.jetstream.api.v1.stream_lookup_response",
  "type": "object",
  "required": ["stream"],
  "properties": {
    "stream": {
      "type": "string",
      "description": "The name of the stream that matched the criteria, empty when nothing is found"
    },
    "is_filtered": {
      "type": "boolean",
      "description": "True when the Stream is matching the subject as a subset of it's ingest subjects"
    }
  }
}
